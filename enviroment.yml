name: bear-hub
channels:
  - bioconda
  - conda-forge
  - defaults
dependencies:
  - python=3.11
  - openjdk=11
  - nextflow
  - git
  - pip
  - pip:
      - streamlit>=1.30
      - pyyaml
      - pandas

