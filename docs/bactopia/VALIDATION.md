# BEAR-HUB ↔ Bactopia 4.0 — Real-data validation

Validation of the Reflex BEAR-HUB command generation against real sequencing data,
run through the actual Bactopia 4.0 pipeline via Docker.

## Test data
- **Short reads (Illumina PE):** `Streptococcus pyogenes` (pyogenes-10, 151bp, ~9MB/read)
- **Long reads (ONT):** `Enterobacter` (genomas_enterobacter GECL####, 1.7–12kb reads, 304MB–1.5GB)
- **Assembly (FASTA):** ref_strains/BEN2908.fa

## Bugs found ONLY by running real data (unit tests could not catch these)

| # | Bug | Symptom | Fix |
|---|---|---|---|
| 1 | `--ident_min`/`--coverage_min` (AMRFinder) + `--scheme`/`--minscore`/`--nopath` (MLST) emitted | `Parameter ident_min is not declared` — Nextflow aborts | These are Bactopia **Tools** params, not main-pipeline. Removed; tools run with defaults. |
| 2 | FOFN had a single `extra` column | `Missing required column(s) 'se', 'ont', 'assembly'` | Rewrote `build_fofn` to the canonical 9-column header `sample runtype genome_size species r1 r2 se ont assembly` (matches `bactopia prepare`). |
| 3 | `_collect()` used `Path.resolve()` (follows symlinks) | symlinked data mis-classified (ONT→single-end, hybrid not merged) | Switched to `os.path.abspath` (no symlink follow). |
| 4 | `-profile` hardcoded to docker | UI profile selector ignored | Threaded `self.profile` into `_main_cmd`. |
| 5 | `--max_memory "16 GB"` (space → quoted) | malformed value | `16.GB` dotted notation. |

> **Lesson:** the param audit JSON summed *all* module params (incl. bactopia-tools),
> so Tools-only params looked valid. Only a real run distinguishes main-pipeline from
> Tools params, and only real data exercises the FOFN format + symlink handling.

## End-to-end result — short reads ✅

Command generated by BEAR-HUB (PE Unicycler, 4 cpus, 8GB, docker) ran the **full
Bactopia 4.0 pipeline successfully**:

```
[SUCCESS] completed=11 failed=0 cached=0   Duration: 3m 51s   Success: true
```

Outputs produced and verified:
- **Assembly:** 67 contigs, **1,783,095 bp** (expected ~1.8 Mb for S. pyogenes), N50 38,595
- **MLST:** `spyogenes ST 28, PERFECT, score 100`
- **AMRFinderPlus:** ran with internal defaults (confirms removing top-level AMR params was correct)
- **Annotation (Prokka), Sketcher (mash/sourmash):** completed

## End-to-end result — long reads (ONT) ✅

Command generated by BEAR-HUB (ONT Dragonflye, 8 cpus, 16GB, docker) on real
*Enterobacter* nanopore data (`GECL0527`, 304 MB) ran the **full pipeline
successfully**:

```
[SUCCESS] completed=11 failed=0 cached=0   Duration: 16m 28s   Success: true
```

Stages (FOFN `runtype=ont`, no `--use_unicycler`, no `--fastp_opts`):
`GATHER → QC (nanoq, not fastp) → ASSEMBLER (Dragonflye/Flye) → Prokka → MLST`.

Outputs produced and verified:
- **Assembly:** 6 contigs, **5,496,991 bp** (~5.5 Mb, expected for *E. cloacae*;
  near-complete — far more contiguous than short-read)
- **MLST:** `ecloacae ST 121, PERFECT, score 100`
- `runtype=ont` propagated correctly end-to-end (long-read QC path, no fastp).

## Hybrid (PE + ONT) — contract validated ✅ (biological run deferred)

No matched PE+ONT data for one strain is available (pyogenes = PE-only,
Enterobacter = ONT-only), so a *biologically* meaningful hybrid assembly can't be
run yet. The **BEAR-HUB → Bactopia contract** was validated with a synthetic
hybrid (pyogenes PE + Enterobacter ONT under one sample name):

- FOFN: single merged sample, `runtype=hybrid`, `r1`+`r2`+`ont` all populated.
- Command: `--fastp_opts …` (kept — hybrid has Illumina) + `--use_unicycler
  --unicycler_mode normal`.
- Bactopia **accepted** it (no param errors): `GATHER → CSVTK_CONCAT → QC →
  ASSEMBLER:ASSEMBLER_MODULE` started; `use_unicycler: true`; meta
  `runtype=hybrid, is_paired=true`. Run then stopped (mismatched organisms →
  assembly would be meaningless).

Dragonflye `--short_polish` runtype is covered by the unit suite
(`test_realdata.py` scenario C). **TODO:** a full biological hybrid run once
matched PE+ONT data for one strain is available.

## Typing/annotation params (main pipeline uses PREFIXED names)

The main pipeline DOES accept typing/annotation params, but prefixed:
`amrfinderplus_*`, `mlst_*`, `prokka_*`, `bakta_*` (unprefixed = Tools-only).

| Param | How it's passed | Why |
|---|---|---|
| `mlst_scheme/minid/mincov/minscore`, `prokka_*`, `amrfinderplus_organism` | CLI flags | strings/integers/bools accepted from CLI |
| `amrfinderplus_ident_min`, `amrfinderplus_coverage_min` | **`-params-file bactopia-params.json`** | FLOAT params — nf-schema rejects floats from the CLI ("string but should be number"); JSON preserves the `number` type. Also, Bactopia already passes these internally, so `--amrfinderplus_opts` can't re-pass them ("used more than once"). |
| `use_bakta` | CLI, requires `--bakta_db` | semantic requirement |

Real runs confirmed: `mlst_scheme=spyogenes`+`minid=95` → ST28 PERFECT;
`-params-file {ident_min:0.9, coverage_min:0.6}` accepted, Success: true.

## Phase 3 — Bactopia Tools (`--wf`) param correctness

The Tools used **wrong (unprefixed) param names** in `catalog.build_tool_args`
(e.g. `--ident_min`, `--minid`, `--pf_mincov`, `--bb`, `--traits`). Rewrote
against each tool's `nextflow_schema.json`: every name is now the schema's
prefixed form (`--amrfinderplus_ident_min`, `--mlst_minid`,
`--plasmidfinder_mincov`, `--iqtree_bb`, `--scoary_traits`, …). `rgi` was the
only tool already correct.

`build_tool_args` now returns `(cli_args, json_params)`:
- **CLI args** — strings/ints/bools (prefixed names).
- **`-params-file` JSON** — `number`-typed params (floats nf-schema rejects from
  the CLI): `amrfinderplus_ident_min/coverage_min`, `plasmidfinder_mincov/threshold`,
  `panaroo_threshold/core_threshold`, `scoary_p_value_cutoff` (`FLOAT_TOOL_PARAMS`).
- Numeric/text fields default to **empty** (help shows the Bactopia default) and
  are emitted only when the user sets them, so Bactopia keeps its own defaults.

### Bug found ONLY by running real `--wf` data — Nextflow 26 empty `Path?`

Bactopia 4.0 tools statically type some inputs as `Path?` (e.g.
`amrfinderplus_db : Path?`) while the schema default is `""`. Nextflow 26
coerces `""`→Path at param resolution and **aborts the whole run** before any
process: `ERROR ~ Path string cannot be empty` (`completed=0 failed=0`). Fix:
pass the param as JSON **`null`** via the `-params-file` (`TOOL_NULL_PATHS`,
mapped for all ~30 tools). Verified: with `{"amrfinderplus_db": null}` the run
proceeds → `AMRFINDERPLUS_RUN` → `CSVTK_CONCAT`, `pyogenes-10.tsv` produced.

### End-to-end Tool runs (pyogenes-10, docker) ✅/⚠️

| Tool | Result | Notes |
|---|---|---|
| `amrfinderplus` | ✅ runs | `amrfinderplus_db: null` → internal DATASETS fallback; floats via params-file |
| `plasmidfinder` | ✅ runs | no db param; `PLASMIDFINDER_MODULE`→`CSVTK_CONCAT`, tsv/json/txt produced |
| `rgi` | ✅ runs | no Path param; `RGI_MAIN`→`CSVTK_CONCAT`→`RGI_HEATMAP`, tsv/json produced |
| `emmtyper` | ✅ runs | **non-detailed** tool — proves the null-fix generalizes to all ~30 tools, not just the 6 with cards. `emmtyper_blastdb: null` → ran on pyogenes-10 → **EMM1.0** (valid emm-type) |
| `mlst` | ⚠️ needs db | module input `db: Path` is **non-nullable** (no DATASETS fallback) — the tool **requires** a user-supplied `--mlst_db` (PubMLST dir/.tar.gz). Exposed as a `path` field with a Browse picker. |

### How a user passes paths (DBs, references, queries)

Paths reach Bactopia as `--<param> /abs/path` CLI args. Because Nextflow gives
the CLI precedence over the `-params-file`, a user value overrides the `null` we
inject. In the UI they're entered via **`path`-kind fields** (added `--mlst_db`),
or — for tools not yet given a detailed card — the **global "Extra args"** box
(`--<param> /path`). As each tool gets a detailed card, its required path inputs
get dedicated `path` fields.

## Test suites (run from bearhub_rx/)
- `tests/test_unit.py` — 100 unit assertions
- `tests/test_params.py` — 52 parameter-flow checks (every UI param → command)
- `tests/test_realdata.py` — 15 real-data FOFN classification checks
- `tests/test_ui.py` — 63 CDP/headless-Chrome UI checks
